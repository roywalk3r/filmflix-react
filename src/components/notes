<>
  {/* Recommended Movies */}
  <h2 className="download-title">You Might Also Like...</h2>
  <Swiper
    modules={[Navigation, EffectCards, Autoplay, Pagination]}
    spaceBetween={10}
    slidesPerView={1}
    navigation={{
      nextEl: ".swiper-button-next",
      prevEl: ".swiper-button-prev",
    }}
    autoplay={{ delay: 3000, disableOnInteraction: true }}
    grabCursor={true}
    effect="cards" // or 'cards' based on your preference
  >
    <div className="recommended_swiper">
      {recommendedMovies.map((r: any) => (
        <SwiperSlide key={r.id}>
          <div className="swiper-slide">
            <img
              src={`https://image.tmdb.org/t/p/original/${r.poster_path}`}
              alt="image poster"
            />
            <div className="box-text">
              <a className="watch-btn play-btn">
                <i className="bx bx-right-arrow"></i>
              </a>
            </div>
          </div>
        </SwiperSlide>
      ))}
    </div>
    {/* Add navigation buttons if needed */}
    <div className="swiper-button-next"></div>
    <div className="swiper-button-prev"></div>
  </Swiper>
</>;
